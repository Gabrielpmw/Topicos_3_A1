@model restaurante.ViewModels.PerfilViewModel

<h3 style="color: var(--gold); margin-bottom: 25px;">Meus Dados</h3>

<form asp-controller="Conta" asp-action="AtualizarPerfil" method="post">
    <div class="form-row">
        <div class="form-group">
            <label>Nome</label>
            <input asp-for="Nome" class="form-input" required>
            </div>
            <div class="form-group">
                <label>Sobrenome</label>
                <input asp-for="Sobrenome" class="form-input" required>
                </div>
            </div>

            <div class="form-group">
                <label>Nome de Usuário</label>
                <input asp-for="NomeUsuario" class="form-input" required>
                </div>

                <div class="form-group">
                    <label>CPF</label>
                    <input asp-for="CPF" class="form-input" required>
                    </div>

                    <div class="form-group">
                        <label>Nova Senha (deixe em branco para manter)</label>
                        <input asp-for="NovaSenha" type="password" class="form-input" placeholder="******">
                        </div>

                        <div class="form-group">
                            <label>Confirmar Nova Senha</label>
                            <input asp-for="ConfirmarNovaSenha" type="password" class="form-input" placeholder="******">
                            </div>

                            <button type="submit" class="btn-gold" style="width: 200px; margin-top: 10px;">
                                Salvar Alterações
                            </button>
                        </form>

                        <div style="margin-top: 40px; padding-top: 20px; border-top: 1px solid rgba(239, 68, 68, 0.3);">
                            <h4 style="color: #ef4444; margin-bottom: 10px; font-size: 1.1rem;">Zona de Perigo</h4>
                            <p style="color: var(--text-gray); font-size: 0.85rem; margin-bottom: 15px;">
                                Ao desativar sua conta, você perderá o acesso ao sistema imediatamente. Seus pedidos anteriores serão mantidos no histórico do restaurante.
                            </p>
                            <button type="button" onclick="desativarMinhaConta()" class="btn-outline-gold" style="border-color: #ef4444; color: #ef4444; padding: 8px 15px;">
                                Desativar Minha Conta
                            </button>
                        </div>